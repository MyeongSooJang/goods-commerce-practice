version: '3.8'

services:
  jenkins:
    image: jenkins/jenkins:lts-jdk21
    container_name: jenkins
    ports:
      - "9090:8080"      # 8080은 gateway가 사용 중 → 9090으로 접속
      - "50000:50000"    # Agent 연결 포트
    volumes:
      - jenkins_home:/var/jenkins_home
    restart: unless-stopped

volumes:
  jenkins_home: